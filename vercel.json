{
  "buildCommand": "pnpm install && cd packages/types && tsc && cd ../.. && pnpm build",
  "installCommand": "pnpm install",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://ai-insights-api-1.onrender.com"
  }
}
